config FHRE_CLANG
	bool "FHRE clang implementation"
	default n
	depends on FHRE
	---help---
		Reserved for the parallel clang/C implementation of FHRE.  The Rust
		implementation is currently the default path.
