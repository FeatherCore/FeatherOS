config FHRE_RUST
	bool "FHRE Rust implementation"
	default y if FHRE
	depends on FHRE
	---help---
		Build the primary FHRE implementation as no_std Rust.  The crate
		uses only core and local code; it does not depend on external Rust
		packages.
