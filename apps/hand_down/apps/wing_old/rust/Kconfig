#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

config WING_OLD_RUST
	bool "Wing old desktop environment (Rust version, historical)"
	default n
	depends on FHRE_RUST
	---help---
		Historical Wing implementation kept only as a reference.
