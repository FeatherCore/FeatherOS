[package]
name = "wing"
version = "0.1.0"
edition = "2021"

[lib]
name = "wing"
path = "src/lib.rs"
crate-type = ["rlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"

[dependencies]
fhre = { path = "../../fhre/rust" }
